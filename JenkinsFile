pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh "cd C:\"
                echo 'shifted'
            }
        }
    }
}
